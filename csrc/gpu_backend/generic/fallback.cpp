/*
 * fallback.cpp — Generic CPU fallback for FlexKV.
 * Used for CI testing or environments without GPU SDK.
 */
// All logic is handled in Python via GenericBackend.
// This stub exists so the build system has a concrete source file.
