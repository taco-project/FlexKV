diff --git a/python/sglang/srt/managers/cache_controller.py b/python/sglang/srt/managers/cache_controller.py
index 07cc92563..56d89f3ef 100644
--- a/python/sglang/srt/managers/cache_controller.py
+++ b/python/sglang/srt/managers/cache_controller.py
@@ -318,13 +318,19 @@ class HiCacheController:
             self.page_get_func = self._generic_page_get
             self.page_set_func = self._generic_page_set
 
-            if (self.storage_backend_type in ["hf3fs", "mooncake", "eic"]) or (
+            uses_zero_copy = (
+                self.storage_backend_type in ["hf3fs", "mooncake", "eic", "flexkv"]
+            ) or (
                 self.storage_backend_type == "dynamic"
                 and bool(self.storage_config.extra_config.get("interface_v1", 0))
-            ):
+            )
+            if uses_zero_copy:
                 self.page_get_func = self._page_get_zero_copy
                 self.page_set_func = self._page_set_zero_copy
 
+            # Only FlexKV needs token_ids passed via extra_info
+            self._needs_extra_token_ids = self.storage_backend_type == "flexkv"
+
         self.device = self.mem_pool_device.device
         self.layer_num = self.mem_pool_device.layer_num
         self.layer_done_counter = LayerDoneCounter(self.layer_num)
@@ -624,6 +630,16 @@ class HiCacheController:
             if not operation.increment(self.page_size):
                 break  # Operation terminated by controller
 
+    def _make_extra_info(
+        self, prefix_keys, token_ids_slice=None
+    ) -> HiCacheStorageExtraInfo:
+        extra = (
+            {"token_ids": token_ids_slice}
+            if self._needs_extra_token_ids and token_ids_slice is not None
+            else None
+        )
+        return HiCacheStorageExtraInfo(prefix_keys=prefix_keys, extra_info=extra)
+
     def _page_transfer(self, operation):
         # Transfer batch by batch
         prefix_keys = operation.prefix_keys
@@ -633,8 +649,12 @@ class HiCacheController:
                 i * self.page_size : (i + len(batch_hashes)) * self.page_size
             ]
             prev_completed_tokens = operation.completed_tokens
-            # Get one batch token, and update the completed_tokens if succeed
-            extra_info = HiCacheStorageExtraInfo(prefix_keys=prefix_keys)
+            batch_end = min(
+                (i + len(batch_hashes)) * self.page_size, len(operation.token_ids)
+            )
+            extra_info = self._make_extra_info(
+                prefix_keys, operation.token_ids[i * self.page_size : batch_end]
+            )
             self.page_get_func(operation, batch_hashes, batch_host_indices, extra_info)
             # Check termination
             if (
@@ -693,7 +713,7 @@ class HiCacheController:
                     batch_tokens[i : i + self.page_size], last_hash
                 )
                 batch_hashes.append(last_hash)
-            extra_info = HiCacheStorageExtraInfo(prefix_keys=prefix_keys)
+            extra_info = self._make_extra_info(prefix_keys, batch_tokens)
             hit_page_num = self.storage_backend.batch_exists(batch_hashes, extra_info)
             hash_value.extend(batch_hashes[:hit_page_num])
             storage_query_count += hit_page_num * self.page_size
@@ -793,7 +813,12 @@ class HiCacheController:
             ]
             # Set one batch token, and record if success.
             # todo: allow partial success
-            extra_info = HiCacheStorageExtraInfo(prefix_keys=prefix_keys)
+            batch_end = min(
+                (i + len(batch_hashes)) * self.page_size, len(operation.token_ids)
+            )
+            extra_info = self._make_extra_info(
+                prefix_keys, operation.token_ids[i * self.page_size : batch_end]
+            )
             success = self.page_set_func(batch_hashes, batch_host_indices, extra_info)
             if not success:
                 logger.warning(
diff --git a/python/sglang/srt/mem_cache/storage/backend_factory.py b/python/sglang/srt/mem_cache/storage/backend_factory.py
index 4b195c863..001f20049 100644
--- a/python/sglang/srt/mem_cache/storage/backend_factory.py
+++ b/python/sglang/srt/mem_cache/storage/backend_factory.py
@@ -181,7 +181,7 @@ class StorageBackendFactory:
 
             dtype = mem_pool_host.dtype
             return backend_class.from_env_config(bytes_per_page, dtype, storage_config)
-        elif backend_name == "eic":
+        elif backend_name in ("eic", "flexkv"):
             return backend_class(storage_config, mem_pool_host)
         else:
             raise ValueError(f"Unknown built-in backend: {backend_name}")
@@ -221,3 +221,9 @@ StorageBackendFactory.register_backend(
     "sglang.srt.mem_cache.storage.eic.eic_storage",
     "EICStorage",
 )
+
+StorageBackendFactory.register_backend(
+    "flexkv",
+    "flexkv.integration.sglang.hicache_storage_adapter",
+    "FlexKVHiCacheStorage",
+)
diff --git a/python/sglang/srt/server_args.py b/python/sglang/srt/server_args.py
index 526465ee5..921116936 100644
--- a/python/sglang/srt/server_args.py
+++ b/python/sglang/srt/server_args.py
@@ -3243,7 +3243,7 @@ class ServerArgs:
         parser.add_argument(
             "--hicache-storage-backend",
             type=str,
-            choices=["file", "mooncake", "hf3fs", "nixl", "aibrix", "dynamic", "eic"],
+            choices=["file", "mooncake", "hf3fs", "nixl", "aibrix", "dynamic", "eic", "flexkv"],
             default=ServerArgs.hicache_storage_backend,
             help="The storage backend for hierarchical KV cache. "
             "Built-in backends: file, mooncake, hf3fs, nixl, aibrix. "
